# Zufaelliger Schluessel fuer die Sitzungs-Cookies.
# Erzeugen mit:  npx auth secret     (oder irgendeine lange zufaellige Zeichenkette)
AUTH_SECRET=

# Discord Developer Portal -> OAuth2
# Redirect-URL dort eintragen:
#   lokal      http://localhost:3000/api/auth/callback/discord
#   auf Vercel https://DEINE-DOMAIN.vercel.app/api/auth/callback/discord
AUTH_DISCORD_ID=
AUTH_DISCORD_SECRET=

# Supabase: Connect -> Direct connection string -> Transaction pooler.
# Fuer dieses Projekt ist das:
#   postgresql://postgres.DEIN-PROJEKT-REF:PASSWORT@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
# PASSWORT ist das Datenbank-Passwort von der Projekterstellung. Supabase zeigt
# es nachtraeglich nicht mehr an - notfalls unter Database -> Settings neu setzen.
DATABASE_URL=

# Wird nicht mehr gebraucht - der Zugang haengt jetzt an den Servern, auf
# denen der Bot ist. Kann leer bleiben.
DISCORD_GUILD_ID=

# Betreiber des Bots - sieht ALLE Server (Komma-getrennt).
# Fuer den normalen Betrieb reicht es, das leer zu lassen: ins Dashboard kommt,
# wer auf einem Discord-Server "Server verwalten" darf, und wen ein Offizier
# dort unter "Zugang" freischaltet.
OFFICER_IDS=
